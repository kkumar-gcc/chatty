<div
  class="offcanvas  offcanvas-end"
  tabindex="-1"
  id="offcanvasContact"
  aria-labelledby="offcanvasContactLabel"
>
  <div class="offcanvas-header">
    <button
      type="button"
      class="btn-close text-back"
      data-bs-dismiss="offcanvas"
      aria-label="Close"
    ></button>

    <!-- <h5 class="offcanvas-title" id="offcanvasExampleLabel">Offcanvas</h5> -->
  </div>
  <div class="offcanvas-body">
    <div class="offcanvas-body">
      <div class="profile2  profile-<%=receiver.profileNum %>">
        <h2 class="profile-tag2"><%= receiver.username.substring(0,2).toUpperCase()%></h2>
      </div>
      <p><%=receiver.name%></p>
    </div>
  
  </div>
</div>
